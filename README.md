# flight

Flight-based applications:
* tracking flight prices (files `index.js` and `index.html`)
* searching for flights to destination cities with forecasted comfortable weather (file `weather.js`)
